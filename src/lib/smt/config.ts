import type { ProductionInputConfig } from '@/lib/production-input/types'

export const SMT_PRODUCTION_INPUT_CONFIG: ProductionInputConfig = {
  productKindLabel: 'SMT',
  fetchErrorTitle: 'SMT 데이터를 불러오지 못했습니다',
  qtyInputId: 'smt-qty-input',
  productionModule: 'smt',
}
