export const SMT_PLAN_LINE_COUNT = 7

export const SMT_PLAN_LINE_NOS = Array.from({ length: SMT_PLAN_LINE_COUNT }, (_, index) => index + 1)

export const SMT_PLAN_DRAG_MIME = 'application/x-mirae-smt-plan'
